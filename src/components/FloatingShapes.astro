<div class="global-floating--glow"></div>
<div class="global-floating--square"></div>

<style>
    .global-floating--square {
        position: fixed;
        top: 3rem;
        left: 3rem;
        bottom: 3rem;
        right: 3rem;
        border: 2px solid var(--accent);
        background: transparent;
        opacity: 0.5;
        pointer-events: none;
        z-index: 10;
    }
    
    .global-floating--glow {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        background: radial-gradient(circle at center, rgba(255, 255, 255, 0.1), transparent 70%);
        z-index: -1;
    }

    @keyframes floatAnimation {
        0% {
        transform: translate(0, 0);
        }
        50% {
        transform: translate(20px, 20px);
        }
        100% {
        transform: translate(0, 0);
        }
    }